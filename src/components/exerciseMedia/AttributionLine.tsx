import { GYMVISUAL_ATTRIBUTION } from '../../lib/exerciseImages'

/** Required credit line whenever dataset media (GIF/thumbnail) is shown. */
export function AttributionLine() {
  return <p className="type-eyebrow example__credit">{GYMVISUAL_ATTRIBUTION}</p>
}
